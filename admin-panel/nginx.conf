# ─── AByte Admin Panel — Nginx Config ───────────────────────────────────────
# Copy this to: /etc/nginx/sites-available/abyte-admin
# Then: ln -s /etc/nginx/sites-available/abyte-admin /etc/nginx/sites-enabled/
# Then: nginx -t && systemctl reload nginx

# ── Frontend (React) ──────────────────────────────────────────────────────────
server {
    listen 80;
    server_name admin.yourdomain.com;

    # Redirect HTTP → HTTPS (enable after SSL setup)
    # return 301 https://$host$request_uri;

    root /var/www/abyte-pos/admin-panel/frontend/dist;
    index index.html;

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml;
    gzip_min_length 1024;

    # Cache static assets
    location ~* \.(js|css|png|jpg|svg|ico|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # React SPA — all routes to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}

# ── Backend API ───────────────────────────────────────────────────────────────
server {
    listen 80;
    server_name admin-api.yourdomain.com;

    # Redirect HTTP → HTTPS (enable after SSL setup)
    # return 301 https://$host$request_uri;

    location /api/ {
        proxy_pass         http://127.0.0.1:5001;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}

# ─── HTTPS (uncomment after: certbot --nginx -d admin.yourdomain.com -d admin-api.yourdomain.com)
# server {
#     listen 443 ssl http2;
#     server_name admin.yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/admin.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/admin.yourdomain.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     root /var/www/abyte-pos/admin-panel/frontend/dist;
#     index index.html;
#
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml;
#
#     location ~* \.(js|css|png|jpg|svg|ico|woff2)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }
#
# server {
#     listen 443 ssl http2;
#     server_name admin-api.yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/admin.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/admin.yourdomain.com/privkey.pem;
#
#     location /api/ {
#         proxy_pass         http://127.0.0.1:5001;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 60s;
#     }
# }
